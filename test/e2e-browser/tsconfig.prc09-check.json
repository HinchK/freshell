{
  "//": "PRC09 item-scoped typecheck gate (same pattern as tsconfig.restore01-check.json / tsconfig.term04-check.json). The repo-owned typecheck configs deliberately exclude test/, so this gives the paced-restore convergence spec an executable gate: run `npx tsc -p test/e2e-browser/tsconfig.prc09-check.json`, then require zero error lines attributable to the spec file. Errors in pre-existing dependency files reproduce identically on the base commit and are out of scope.",
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "incremental": false,
    "types": ["node"]
  },
  "include": [
    "specs/paced-restore-convergence-rust.spec.ts"
  ]
}
